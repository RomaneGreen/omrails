<%= form_for([@tweet,@tweet.comments.build]) do |form| %>
<div class="form-group">
  <%= form.label :comments? %>
  <%= form.text_field :content,class:"form-control" %>
</div>

<div class="actions">
  <%= form.submit class:"btn btn-primary" %>
</div>
<% end %>